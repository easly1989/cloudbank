openapi: 3.1.0
info:
  title: CloudBank API
  version: 0.1.0
  description: >
    HTTP API for CloudBank, a self-hosted web port of HomeBank. This document is
    the single source of truth for the contract: the Go server interfaces and
    the TypeScript client types are both generated from it. Resources are added
    milestone by milestone; this initial version covers the health and ping
    endpoints established by the server skeleton.
  license:
    name: AGPL-3.0-or-later
    url: https://www.gnu.org/licenses/agpl-3.0.html
servers:
  - url: /
paths:
  /healthz:
    get:
      operationId: getHealth
      summary: Liveness/readiness probe
      description: Returns 200 when the server (and its database, once wired) is healthy.
      security: []
      responses:
        "200":
          description: Service is healthy.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
        "503":
          description: A dependency is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
  /api/v1/ping:
    get:
      operationId: getPing
      summary: Connectivity check
      responses:
        "200":
          description: Pong.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: pong
                required: [message]
  /api/v1/setup/status:
    get:
      operationId: getSetupStatus
      summary: First-run setup status
      description: Reports whether the instance has no users yet and needs the setup wizard.
      security: []
      responses:
        "200":
          description: Setup status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  needsSetup:
                    type: boolean
                required: [needsSetup]
  /api/v1/setup:
    post:
      operationId: postSetup
      summary: Create the first (admin) user
      description: Allowed only when no user exists yet; creates an administrator and starts a session.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Credentials"
      responses:
        "201":
          description: Admin created and logged in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400": { $ref: "#/components/responses/BadRequest" }
        "409":
          description: Setup has already been completed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/auth/login:
    post:
      operationId: login
      summary: Log in
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Credentials"
      responses:
        "200":
          description: Logged in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429":
          description: Too many attempts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/auth/logout:
    post:
      operationId: logout
      summary: Log out (revoke the current session)
      responses:
        "204":
          description: Logged out.
  /api/v1/auth/me:
    get:
      operationId: getMe
      summary: Current authenticated user
      responses:
        "200":
          description: The current user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/admin/users:
    get:
      operationId: listUsers
      summary: List users (admin)
      responses:
        "200":
          description: All users.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUser
      summary: Create a user (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserRequest"
      responses:
        "201":
          description: User created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: Username already taken.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/admin/users/{id}/disable:
    post:
      operationId: setUserDisabled
      summary: Enable or disable a user (admin)
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                disabled: { type: boolean }
              required: [disabled]
      responses:
        "204": { description: Updated. }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/users/{id}/password:
    post:
      operationId: resetUserPassword
      summary: Reset a user's password (admin)
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                password: { type: string, minLength: 8 }
              required: [password]
      responses:
        "204": { description: Password reset. }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/wallets:
    get:
      operationId: listWallets
      summary: List the current user's wallets
      responses:
        "200":
          description: The user's wallets.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Wallet"
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      operationId: createWallet
      summary: Create a wallet (creator becomes owner)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WalletInput"
      responses:
        "201":
          description: Wallet created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wallet"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/wallets/{walletId}:
    parameters:
      - { name: walletId, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: getWallet
      summary: Get a wallet
      responses:
        "200":
          description: The wallet.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wallet"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateWallet
      summary: Rename a wallet (owner only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WalletInput"
      responses:
        "200":
          description: Updated wallet.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wallet"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      operationId: deleteWallet
      summary: Delete a wallet and all its data (owner only)
      responses:
        "204": { description: Deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/catalog/currencies:
    get:
      operationId: getCurrencyCatalog
      summary: The ISO 4217 currency catalog
      responses:
        "200":
          description: Catalog entries.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CatalogEntry"
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/wallets/{walletId}/currencies:
    parameters:
      - { name: walletId, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: listCurrencies
      summary: List a wallet's currencies
      responses:
        "200":
          description: Currencies (base first).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Currency"
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: addCurrency
      summary: Add a currency to a wallet
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddCurrencyRequest"
      responses:
        "201":
          description: Currency added.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Currency"
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Currency already added.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/wallets/{walletId}/currencies/{currencyId}:
    parameters:
      - { name: walletId, in: path, required: true, schema: { type: integer, format: int64 } }
      - { name: currencyId, in: path, required: true, schema: { type: integer, format: int64 } }
    patch:
      operationId: updateCurrency
      summary: Update a currency's rate and/or formatting
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CurrencyUpdate"
      responses:
        "200":
          description: Updated currency.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Currency"
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      operationId: deleteCurrency
      summary: Remove a currency (not the base)
      responses:
        "204": { description: Deleted. }
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/wallets/{walletId}/currencies/{currencyId}/base:
    parameters:
      - { name: walletId, in: path, required: true, schema: { type: integer, format: int64 } }
      - { name: currencyId, in: path, required: true, schema: { type: integer, format: int64 } }
    post:
      operationId: setBaseCurrency
      summary: Make a currency the wallet's base
      responses:
        "204": { description: Base currency set. }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/wallets/{walletId}/currencies/{currencyId}/rates:
    parameters:
      - { name: walletId, in: path, required: true, schema: { type: integer, format: int64 } }
      - { name: currencyId, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: getCurrencyRates
      summary: A currency's exchange-rate history
      responses:
        "200":
          description: Rate history (newest first).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ExchangeRate"
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/wallets/{walletId}/accounts:
    parameters:
      - { name: walletId, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: listAccounts
      summary: List a wallet's accounts
      responses:
        "200":
          description: Accounts (ordered by position/group/name).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Account"
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createAccount
      summary: Create an account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountInput"
      responses:
        "201":
          description: Account created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Account name already used.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/wallets/{walletId}/accounts/reorder:
    parameters:
      - { name: walletId, in: path, required: true, schema: { type: integer, format: int64 } }
    post:
      operationId: reorderAccounts
      summary: Reorder accounts (positions and groups)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                type: object
                properties:
                  id: { type: integer, format: int64 }
                  position: { type: integer }
                  groupName: { type: string }
                required: [id, position]
      responses:
        "204": { description: Reordered. }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/wallets/{walletId}/accounts/{accountId}:
    parameters:
      - { name: walletId, in: path, required: true, schema: { type: integer, format: int64 } }
      - { name: accountId, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: getAccount
      summary: Get an account
      responses:
        "200":
          description: The account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateAccount
      summary: Update an account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountInput"
      responses:
        "200":
          description: Updated account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Account name already used.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteAccount
      summary: Delete an account and its data
      responses:
        "204": { description: Deleted. }
        "404": { $ref: "#/components/responses/NotFound" }
components:
  responses:
    BadRequest:
      description: Invalid request.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Authentication required.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: Administrator access required.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
  schemas:
    Health:
      type: object
      properties:
        status:
          type: string
          enum: [ok, unhealthy]
        error:
          type: string
      required: [status]
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
          required: [code, message]
      required: [error]
    User:
      type: object
      properties:
        id: { type: integer, format: int64 }
        username: { type: string }
        email: { type: string }
        isAdmin: { type: boolean }
        locale: { type: string }
        theme: { type: string }
        disabled: { type: boolean }
        createdAt: { type: string }
      required: [id, username, email, isAdmin, locale, theme, disabled, createdAt]
    Credentials:
      type: object
      properties:
        username: { type: string }
        email: { type: string }
        password: { type: string, minLength: 8 }
      required: [username, password]
    CreateUserRequest:
      type: object
      properties:
        username: { type: string }
        email: { type: string }
        password: { type: string, minLength: 8 }
        isAdmin: { type: boolean }
      required: [username, password]
    Wallet:
      type: object
      properties:
        id: { type: integer, format: int64 }
        title: { type: string }
        ownerName: { type: string }
        baseCurrencyId: { type: integer, format: int64, nullable: true }
        role: { type: string, enum: [owner, member] }
        createdAt: { type: string }
      required: [id, title, ownerName, role, createdAt]
    WalletInput:
      type: object
      properties:
        title: { type: string }
        ownerName: { type: string }
        baseCurrency: { type: string, description: "ISO 4217 code for the base currency (create only)" }
      required: [title]
    CatalogEntry:
      type: object
      properties:
        code: { type: string }
        name: { type: string }
        symbol: { type: string }
        fracDigits: { type: integer }
        symbolPrefix: { type: boolean }
      required: [code, name, symbol, fracDigits, symbolPrefix]
    Currency:
      type: object
      properties:
        id: { type: integer, format: int64 }
        isoCode: { type: string }
        name: { type: string }
        symbol: { type: string }
        symbolPrefix: { type: boolean }
        decimalChar: { type: string }
        groupChar: { type: string }
        fracDigits: { type: integer }
        isBase: { type: boolean }
        rate: { type: number }
        rateUpdatedAt: { type: string }
      required: [id, isoCode, name, symbol, symbolPrefix, decimalChar, groupChar, fracDigits, isBase, rate]
    AddCurrencyRequest:
      type: object
      properties:
        isoCode: { type: string }
        makeBase: { type: boolean }
      required: [isoCode]
    CurrencyUpdate:
      type: object
      properties:
        rate: { type: number }
        symbol: { type: string }
        symbolPrefix: { type: boolean }
        decimalChar: { type: string }
        groupChar: { type: string }
        fracDigits: { type: integer }
    ExchangeRate:
      type: object
      properties:
        date: { type: string }
        rate: { type: number }
        source: { type: string }
      required: [date, rate, source]
    Account:
      type: object
      properties:
        id: { type: integer, format: int64 }
        name: { type: string }
        type:
          type: string
          enum: [bank, cash, checking, savings, creditcard, liability, asset, investment]
        currencyId: { type: integer, format: int64 }
        institution: { type: string }
        number: { type: string }
        initialBalance: { type: integer, format: int64, description: "minor units" }
        minimumBalance: { type: integer, format: int64, description: "minor units" }
        balance: { type: integer, format: int64, description: "minor units" }
        closed: { type: boolean }
        noSummary: { type: boolean }
        noBudget: { type: boolean }
        noReport: { type: boolean }
        position: { type: integer }
        groupName: { type: string }
        notes: { type: string }
        website: { type: string }
        createdAt: { type: string }
        currencyCode: { type: string }
        currencySymbol: { type: string }
        currencySymbolPrefix: { type: boolean }
        currencyDecimalChar: { type: string }
        currencyGroupChar: { type: string }
        currencyFracDigits: { type: integer }
      required:
        [id, name, type, currencyId, initialBalance, minimumBalance, balance, closed, position,
         currencyCode, currencySymbol, currencySymbolPrefix, currencyDecimalChar, currencyGroupChar, currencyFracDigits]
    AccountInput:
      type: object
      properties:
        name: { type: string }
        type:
          type: string
          enum: [bank, cash, checking, savings, creditcard, liability, asset, investment]
        currencyId: { type: integer, format: int64, description: "defaults to the wallet base currency" }
        institution: { type: string }
        number: { type: string }
        initialBalance: { type: integer, format: int64 }
        minimumBalance: { type: integer, format: int64 }
        closed: { type: boolean }
        noSummary: { type: boolean }
        noBudget: { type: boolean }
        noReport: { type: boolean }
        groupName: { type: string }
        notes: { type: string }
        website: { type: string }
      required: [name, type]
