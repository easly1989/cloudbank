import type { ReportContext } from "./reportContext";

// The tab itself comes with #492; the frame (#489) only needs its name.
export function BalancesTab({ ctx }: { ctx: ReportContext }) {
  void ctx;
  return null;
}
