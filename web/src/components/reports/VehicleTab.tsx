import type { ReportContext } from "./reportContext";

// The tab itself comes with #493; the frame (#489) only needs its name.
export function VehicleTab({ ctx }: { ctx: ReportContext }) {
  void ctx;
  return null;
}
