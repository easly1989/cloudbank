import { createTheme } from "@mantine/core";

// Base Mantine theme. Extended as the design system grows.
export const theme = createTheme({
  primaryColor: "teal",
  fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif",
  defaultRadius: "md",
});
